# file-name-changer

Allows you to change the filename of a group of files in a directory.

Move the files you want to rename to the files-to-rename directory.

Configure the const variables in index.js to get your file(s) looking the way you want.

Run the program by running npm run start
